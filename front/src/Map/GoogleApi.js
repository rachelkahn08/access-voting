export default class GoogleApi {
	generateScript(url) {

	}

	getDirections() {
		
	}

	componentWillMount() {
		

		// const script = document.createElement("script");

  //       script.src = this.GoogleApi.url + this.GoogleApi.key;
  //       script.async = true;

  //       document.body.appendChild(script);
	}

	componentDidMount() {
		
	}

	shouldComponentUpdate() {
		// return loading ? true : false;
	}

	// render() {
	// 	return <script src={ this.buildURL() } async defer />
	// }
}